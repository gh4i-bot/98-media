import SiteHeader from '../components/SiteHeader';import{posts}from'./posts';export const metadata={title:'Field Notes | 98 Media'};export default function Blog(){return <main><SiteHeader/><section className="blog-hero shell"><div className="section-tag">Field notes / Meta ads</div><h1>Read the signal.<br/><em>Make the next move.</em></h1><p>Practical notes on creative, campaign economics, and turning market response into the next decision.</p></section><section className="blog-list shell">{posts.map((p,i)=><a className="blog-card" href={`/blog/${p.slug}`} key={p.slug}><span>{String(i+1).padStart(2,'0')} / {p.module}</span><h2>{p.title}</h2><p>{p.dek}</p><b>Read note ↗</b></a>)}</section></main>}
