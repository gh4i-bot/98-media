import{notFound}from'next/navigation';import SiteHeader from'../../components/SiteHeader';import{posts}from'../posts';export function generateStaticParams(){return posts.map(({slug})=>({slug}))}export default async function Post({params}){const{slug}=await params,p=posts.find(x=>x.slug===slug);if(!p)notFound();return <main><SiteHeader/><article className="post shell"><div className="section-tag">Field notes / {p.module}</div><h1>{p.title}</h1><p className="post-dek">{p.dek}</p><div className="post-body">{p.body.map(x=><p key={x}>{x}</p>)}</div></article></main>}
